// Academia Arcana Victory — GitHub Pages Reader
// Fill in your GitHub username and repository name before deploying.

const CONFIG = {
  GITHUB_USER: 'YOUR_GITHUB_USERNAME',   // e.g. 'lupires'
  GITHUB_REPO: 'YOUR_REPO_NAME',         // e.g. '3det-academia-arcana'
  GITHUB_BRANCH: 'master',

  // Base path within the repo where the content lives (no leading slash)
  CONTENT_ROOT: 'Academia Arcana Victory',
};
